# Mapbox Access Token required for map base layers
VITE_MAPBOX_ACCESS_TOKEN=''

# Debug Mode - set to 1 to enable additional console logging
VITE_DEBUG_VIEWER=0

# Target the viewer at a specific ESP GIS Server deployment e.g 'https://myEspServer.com/viewer'
# Ensure you include the protocol (e.g. https://) and the `/viewer` this will be overwritten parsed
VITE_SERVER_TARGET_OVERRIDE=''